---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Mis promos | Registro">
  <section class="grid gap-8 lg:grid-cols-[1.1fr,0.9fr]">
    <div class="space-y-5">
      <div class="space-y-3">
        <p class="text-sm uppercase tracking-[0.3em] text-ink-900/60">Negocios</p>
        <h1 class="text-3xl font-display">Crea tu cuenta y activa tus promos</h1>
        <p class="text-sm text-ink-900/60">
          Diseñado para negocios locales. Publica promociones, recibe visitas y mide
          cómo responde tu comunidad.
        </p>
      </div>
      <div class="grid gap-4 md:grid-cols-2">
        <div class="rounded-3xl border border-ink-900/10 bg-white/80 p-5 text-sm text-ink-900/70">
          <p class="text-xs uppercase tracking-[0.2em] text-ink-900/50">Lo que obtienes</p>
          <p class="mt-3 font-semibold text-ink-900">Control total de tu perfil</p>
          <p class="mt-2">Gestiona sedes, horarios y promos en minutos.</p>
        </div>
        <div class="rounded-3xl border border-ink-900/10 bg-white/80 p-5 text-sm text-ink-900/70">
          <p class="text-xs uppercase tracking-[0.2em] text-ink-900/50">Promos en vivo</p>
          <p class="mt-3 font-semibold text-ink-900">Más visibilidad local</p>
          <p class="mt-2">Aparece en búsquedas por ciudad y categoría.</p>
        </div>
        <div class="rounded-3xl border border-ink-900/10 bg-white/80 p-5 text-sm text-ink-900/70">
          <p class="text-xs uppercase tracking-[0.2em] text-ink-900/50">Equipo listo</p>
          <p class="mt-3 font-semibold text-ink-900">Acceso inmediato</p>
          <p class="mt-2">Crea tu cuenta y empieza hoy mismo.</p>
        </div>
        <div class="rounded-3xl border border-ink-900/10 bg-white/80 p-5 text-sm text-ink-900/70">
          <p class="text-xs uppercase tracking-[0.2em] text-ink-900/50">Simple y claro</p>
          <p class="mt-3 font-semibold text-ink-900">Sin pasos largos</p>
          <p class="mt-2">Solo necesitas un correo y contraseña.</p>
        </div>
      </div>
      <div class="rounded-3xl border border-ink-900/10 bg-white/70 p-6 text-sm text-ink-900/70">
        <p class="font-semibold text-ink-900">¿Ya tienes cuenta?</p>
        <p class="mt-2">Ingresa para administrar tus promos activas.</p>
        <a
          href="/login"
          class="mt-4 inline-flex rounded-full border border-ink-900/20 px-4 py-2 font-semibold"
        >
          Iniciar sesión
        </a>
      </div>
    </div>
    <div class="glass-panel rounded-3xl p-6">
      <div class="mb-5 space-y-2">
        <h2 class="text-xl font-semibold">Crear cuenta</h2>
        <p class="text-sm text-ink-900/60">
          Activa tu negocio y empieza a publicar promociones visibles en minutos.
        </p>
      </div>
      <form class="grid gap-4" data-auth-form data-mode="register">
        <label class="grid gap-2 text-sm font-semibold">
          Email
          <input class="input-field" type="email" name="email" placeholder="tu@negocio.com" required />
        </label>
        <label class="grid gap-2 text-sm font-semibold">
          Contraseña
          <input class="input-field" type="password" name="password" minlength="8" required />
        </label>
        <p class="text-xs text-ink-900/50">
          Usa una contraseña de al menos 8 caracteres.
        </p>
        <p class="text-sm text-ink-900/60" data-auth-message></p>
        <button class="button-primary" type="submit">Crear cuenta</button>
      </form>
    </div>
  </section>

  <script type="module" src={Astro.resolve("../scripts/auth.ts")}></script>
</BaseLayout>
