---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Tus promos | Contacto">
  <section
    class="mx-auto max-w-3xl rounded-3xl border border-ink-900/10 bg-white p-6 sm:p-8"
  >
    <h1 class="text-2xl font-semibold">Contacto</h1>
    <p class="mt-2 text-sm text-ink-900/60">
      Escríbenos y te respondemos lo antes posible.
    </p>
    <form class="mt-6 grid gap-4" method="post">
      <label class="grid gap-2 text-sm font-semibold">
        Nombre
        <input class="input-field" name="name" autocomplete="name" required />
      </label>
      <label class="grid gap-2 text-sm font-semibold">
        Correo
        <input
          class="input-field"
          type="email"
          name="email"
          autocomplete="email"
          required
        />
      </label>
      <label class="grid gap-2 text-sm font-semibold">
        Mensaje
        <textarea
          class="input-field min-h-[140px] resize-y"
          name="message"
          required></textarea>
      </label>
      <div class="flex justify-end">
        <button class="button-primary text-xs sm:text-sm" type="submit">
          Enviar mensaje
        </button>
      </div>
    </form>
  </section>
</BaseLayout>
